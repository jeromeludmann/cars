import bodyParser from 'body-parser'

// export const urlencodedBodyParser = bodyParser.urlencoded({
//   extended: true // value can be a string, array or any type
// })

export const jsonBodyParser = bodyParser.json()
